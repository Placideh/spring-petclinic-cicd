apiVersion: v1
kind: Service
metadata:
  name: petclinic-service
  namespace: petclinic-prod
  labels:
    app: petclinic
spec:
  type: NodePort
  selector:
    app: petclinic
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30082
    name: http
